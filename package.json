{
  "name": "@parsajr/mdcompiler",
  "version": "1.0.7",
  "description": "Scan a directory of Markdowns, parse their content, and compile them into one single JSON file for easy access and management.",
  "types": "./dist/index.d.ts",
  "type": "module",
  "bin": {
    "mdcompiler": "./dist/cli.js"
  },
  "exports": {
    "default": "./dist/index.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "dev": "tsc && node ./dist/cli.js",
    "prepare": "pnpm run build",
    "release": "pnpm publish"
  },
  "author": "Parsa Javan",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.14.1",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "citty": "^0.1.6",
    "gray-matter": "^4.0.3",
    "marked": "^15.0.11"
  },
  "keywords": [
    "Markdown",
    "Json"
  ],
  "files": [
    "dist"
  ]
}
